{
  "name": "roboxence-2026",
  "version": "1.0.0",
  "private": true,
  "description": "ROBOXENCE 2026 — Decoupled Frontend and Backend Architecture (Dept. of Robotics & AI, SVPCET Nagpur)",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "keywords": [
    "roboxence-2026",
    "robotics",
    "ai",
    "svpcet",
    "react",
    "express"
  ],
  "author": "Department of Robotics and Artificial Intelligence, SVPCET Nagpur",
  "license": "ISC"
}
